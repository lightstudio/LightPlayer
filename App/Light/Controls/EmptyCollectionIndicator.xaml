<UserControl
    x:Class="Light.Controls.EmptyCollectionIndicator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Light.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel
        VerticalAlignment="Center"
        HorizontalAlignment="Center">
        <FontIcon 
            FontSize="64"
            HorizontalAlignment="Center"
            FontFamily="Segoe MDL2 Assets"
            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            Glyph="&#xE946;"/>
        <TextBlock 
            x:Uid="EmptyCollectionIndicatorTitleTextBlk"
            Style="{StaticResource TitleTextBlockStyle}"
            HorizontalAlignment="Center"
            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            Text="It's lonely here."/>
        <TextBlock
            x:Uid="EmptyCollectionIndicatorSubtitleTextBlk"
            HorizontalAlignment="Center"
            Style="{StaticResource BaseTextBlockStyle}"
            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
            Text="Add something to here to get started."/>
    </StackPanel>
</UserControl>
